# React Email
Build and send emails using React and TypeScript.

## Usage

This will install all required dependencies, and create a sample email template at `email/my-email.tsx`

```sh copy
bun x --bun shadcn@latest add "https://lib0.vercel.app/r/react-email.json"
```
